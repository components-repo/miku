#!/usr/bin/env node
const commander = require('commander');

commander.version('0.1.0')
         .description('create react components by TS CLI');

commander.parse(process.argv);

function init() {
    
}